// this  is the full code to print the acc , gyro , mag - raw lsb values -> using timer loop / interrupt pin / sensor hub ? (as per whats best) 

// simply combine the ISM full code and MMC full code + uisng timer / interrupt pin / sensor hub / offset seen ??

// and also figure out which is good ? 1) MCU->ISM , MCU->MMC or 2) MCU->ISM->MMC using sensor hub...
// and using timer loop or looping using interrupt pin.

// and we use interrupt pin for acc/gyro/mag .... (with or without sensor hub -- i think interrupt pin uses hoga hee... (either int1 & mint // or // int1 & int2 for sesnor_hub ))

// store all the values first before scaling & prining. so that more sync. 



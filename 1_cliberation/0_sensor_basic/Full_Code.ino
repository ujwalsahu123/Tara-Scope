// this  is the full code to print the acc , gyro , mag - raw lsb values .... 

// know simply combine the ISM full code and MMC full code. 
// and also figure out which is good ? 1) MCU->ISM , MCU->MMC or 2) MCU->ISM->MMC using sensor hub...

// and we use interrupt pin for acc/gyro/mag .... (with or without sensor hub -- i think interrupt pin uses hoga hee... (either int1 & mint // or // int1 & int2 for sesnor_hub ))

// get all the values first and store it.  and then scale them ... so that more sync

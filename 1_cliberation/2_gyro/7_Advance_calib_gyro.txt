gyro has 0.4dps error/noise after calib.
acc has ...

later do -> gyro advance calib , and learn more about calib algo ... see star(bookmark) websites of Ujwalsahu1styearEngg account.





gpt read -------------------------gyro advance calib- (later) ---------------------------------------------------


1️⃣ What is advanced IMU / gyro calibration?

What you did so far:

Basic calibration = bias removal

gyro_calibrated = raw - bias


This fixes the biggest error, but it is not full calibration.

Advanced calibration means estimating this full model:

For gyro (same idea for accel):


Where:

b → bias (you already do this ✅)

M → 3×3 matrix that includes:

scale factor errors

axis misalignment

cross-axis coupling

Expanded, it looks like:

[ wx ]   [ sxx  sxy  sxz ] [ ωx ]   [ bx ]
[ wy ] = [ syx  syy  syz ] [ ωy ] + [ by ]
[ wz ]   [ szx  szy  szz ] [ ωz ]   [ bz ]


Bias-only calibration assumes:

M = identity matrix


Advanced calibration estimates M.

2️⃣ What kinds of advanced calibration exist?
🔹 1. Factory / offline calibration (most common)

Done once

Uses:

rate tables

rotation rigs

long controlled motions

Produces:

scale factors

misalignment matrices

Stored in flash

➡️ This is what IMU manufacturers do

🔹 2. In-situ motion-based calibration (research-grade)

This is the paper you mentioned earlier.

Uses:

accelerometer (gravity reference)

known motion constraints

standstill detection

nonlinear optimization

Pros:

No special hardware

Very accurate

Cons:

Complex

Offline

Needs Python / MATLAB

Not real-time

Easy to mess up

🔹 3. Online self-calibration (rare)

EKF estimates bias + scale slowly

Used in:

high-end INS

aerospace systems

❗ Overkill for most projects

3️⃣ What advanced calibration actually improves

Let’s be very honest:

Error source	Bias-only	Advanced calib
Turn-on offset	✅ fixed	✅ fixed
Short-term drift	⚠️ partly	⚠️ partly
Noise	❌	❌
Scale error	❌	✅
Axis misalignment	❌	✅
Cross-axis coupling	❌	✅
Fusion stability	✅ already good	🔺 slightly better

👉 Fusion still dominates accuracy, not calibration.

4️⃣ Should you do advanced calibration?
Short answer

❌ No — not now

Why (very important)

Your use case:

orientation

star pointing

sensor fusion

You already:

removed bias

control sampling

use fusion

➡️ Advanced calibration will give maybe 1–5% improvement,
➡️ Fusion gives 90% of the performance.

5️⃣ When advanced calibration actually makes sense

You should consider it only if:

You integrate gyro for long dead-reckoning

You need sub-degree/hour accuracy

You are doing:

INS

SLAM

robotics navigation

aerospace work

You want factory calibration for many devices

You are doing R&D / research / papers

Then yes — learn it.

6️⃣ What you should do now (correct roadmap)
✅ Now (what you’re doing is perfect)

Bias calibration at startup

Fixed-rate sampling

Sensor fusion (Madgwick / EKF)

Test, validate, ship

🔜 Later (R&D phase)

Allan variance → noise modeling

EKF bias estimation

Optional offline advanced calibration

Store calibration matrices in flash

7️⃣ Final takeaway (remember this)

Bias calibration is mandatory.
Advanced calibration is optional.
Sensor fusion is the real solution.


ok so the max we could do was -> bias calib and then other things like - scale factor errors axis misalignment cross-axis coupling are done by factory calib. so we did our max
and the advance we will do at RnD stage 
so we can learn about factory calib process (see the star bookmark websites) 
and there is also a paper which lets us do advance gyro calib easily "https://github.com/mikoff/imu-calib?tab=readme-ov-file"

so also learn about factor calib process of others also -> mag , acc , gyro all. 